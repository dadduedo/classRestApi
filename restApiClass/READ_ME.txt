per far partire il progetto basta andare nella cartella del file docker-compose.yml
Da terminale far partire il progetto con il comando

docker-compose up

oppure se non avete un ambiente docker andare dentro la cartella src
e premere il comando

npm run start

grazie